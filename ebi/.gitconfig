; .gitconfig
; vim: set filetype=gitconfig :


; https://git-scm.com/book/en/v2/Customizing-Git-Git-Configuration
; man git config


[user]
    ; full name
    name = William Stark
    ; email address
    email = william@ebi.ac.uk
    ; GPG signing key ID
    signingkey =
